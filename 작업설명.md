# 병원 물품 관리 시스템 — 작업 설명

이 문서는 새로 합류하는 개발자가 프로젝트 구조를 빠르게 파악하고 이어서 개발할 수 있도록
정리한 참고 자료입니다. 세부 로직은 결국 코드를 봐야 하지만, "어디를 봐야 하는지"는 이 문서로
찾을 수 있습니다.

## 1. 기술 스택

- **프론트엔드**: React 18 + TypeScript, Vite, Tailwind CSS, Recharts(차트), react-router-dom
- **백엔드**: Express + TypeScript (ts-node로 실행), node-cron(스케줄링)
- **DB**: SQLite(better-sqlite3) + Prisma ORM
- **데스크톱 패키징**: Electron → electron-builder로 Windows(NSIS) 설치파일 빌드
- **문서 생성**: exceljs/xlsx(엑셀), pdfkit(PDF), 자체 hwpx 어댑터(한글 파일)
- **테스트**: Vitest

## 2. 실행 방법

```
npm install
npm run prisma:generate      # Prisma client 생성
npm run dev                  # 서버(5000?) + 클라이언트(vite) 동시 실행
npm test                     # vitest 테스트
npm run electron:dev         # Electron 데스크톱 앱으로 실행
npm run build                # 프로덕션 빌드
npm run electron:build       # Windows 설치파일(.exe) 생성
```

DB는 `prisma/hospital-supply.db` (SQLite 파일, git에는 올라가지 않음 — `.gitignore` 참고).
마이그레이션은 `npm run prisma:migrate`.

## 3. 폴더 구조

```
src/
  client/               # 프론트엔드
    pages/              # 화면 단위 (라우트 1개 = 파일 1개가 기본)
    components/         # 재사용 컴포넌트 (Approval/, Patients/, PurchaseOrders/, stats/, ui/)
    hooks/               # useAuth 등
    config/, utils/
  server/               # 백엔드
    routes/             # Express 라우트 (API 엔드포인트 단위)
    services/           # 비즈니스 로직 (라우트에서 호출)
      doc-engine/        # 문서(엑셀/hwpx) 자동 생성 엔진
      ml/                 # 수요예측 관련 모델링
    db/                  # DB 접근 가드/헬퍼
    middleware/
  shared/                # 프론트/백 공통 타입 등
  electron/              # Electron 메인 프로세스
prisma/
  schema.prisma          # DB 스키마 (모델 60여 개)
  migrations/             # 마이그레이션 히스토리
```

## 4. 화면(라우트) ↔ 기능 매핑

`src/client/App.tsx`에 전체 라우팅이 정의되어 있습니다. 권한(`perm`/`anyPerm`)에 따라
메뉴 노출이 달라집니다.

| 경로 | 화면 | 설명 |
|---|---|---|
| `/` | DashboardPage | 대시보드 |
| `ward-requests` | WardRequestPage | 병동 물품 신청 |
| `equipment-requests` / `my-equipment` | EquipmentRequestPage / MyEquipmentPage | 장비 신청/내 장비 |
| `approvals` | ApprovalPage | 신청 승인 처리 |
| `purchase-orders` | PurchaseHubPage | 발주 허브 (발주라우팅·구매결정 탭 통합, 구 메뉴는 여기로 리다이렉트) |
| `receipts` / `receipt-check` | ReceiptsPage / ReceiptCheckPage | 입고 등록/입고 확인(2단계 검수) |
| `stock-out` | StockOutPage | 출고 처리 |
| `inventory` | InventoryPage | 재고 현황 |
| `dept-inventory` | DeptInventoryPage | **부서별 재고** (신규) |
| `stats` | StatsDashboardPage | 통계 대시보드 (물품/환자/발주 등 탭) |
| `cost-analysis` / `cost-reconcile` | CostAnalysisPage / CostReconcilePage | **원가분석/원가정산** (신규) |
| `items` / `items/recategorize` | ItemsPage / RecategorizePage | 품목 관리 / 품목 재분류 |
| `item-categories` | ItemCategoriesPage | 분류 관리 (사용자가 중분류 직접 추가 가능) |
| `vendors` | VendorsPage | 거래처 관리 |
| `baselines` | BaselinesPage | 사용 기준치 |
| `loans` | LoansPage | 대여 관리 |
| `patient-manage` | PatientManagePage | 환자 관리 |
| `users` / `dept-permissions` | UsersPage / DeptCategoryPage | 사용자/부서별 권한 |
| `doc-templates` | DocTemplatesPage | **문서 템플릿 관리** (신규, 문서엔진 연동) |
| `request-schedules` | RequestSchedulesPage | 정기 신청 스케줄 |
| `treatment-types` | TreatmentTypesPage | 처치 유형 관리 |
| `incineration` | IncinerationPage | 소각 관리 |
| `system` / `audit-logs` | SystemPage / AuditLogPage | 시스템 설정 / 감사 로그 |

## 5. 백엔드 API 라우트 (`src/server/routes/`)

기능 그룹별 정리:

- **인증/권한**: `auth.ts`, `users.ts`, `menu-policies.ts`, `menu-scopes.ts`, `dept-permissions.ts`
- **품목/거래처**: `items.ts`, `master-codes.ts`, `vendors.ts`
- **신청/승인**: `ward-requests.ts`, `equipment-units.ts`, `approvals.ts`, `request-schedules.ts`
- **발주/입출고**: `purchase-orders.ts`, `order-routing.ts`, `purchase-decisions.ts`, `receipts.ts`, `receipt-check.ts`, `stock-out.ts`
- **재고**: `inventory.ts`, `inventory-stats.ts`, `baselines.ts`, `loans.ts`
- **원가**: `cost.ts`, `cost-analysis.ts`, `cost-reconcile.ts`
- **환자**: `patients.ts`, `patient-stats.ts`, `patient-item-mapping.ts`, `disease-codes.ts`, `hira-disease-stats.ts`, `treatment-types.ts`
- **통계**: `dashboard.ts`, `stockout-stats.ts`, `po-stats.ts`, `request-stats.ts`, `supply-analytics.ts`, `demand-forecast.ts`
- **문서엔진**: `doc-templates.ts` (엑셀/hwpx 템플릿 기반 문서 자동생성)
- **기타**: `dept-calendar.ts`, `hira-api.ts`, `ai.ts`(AI 연동), `system.ts`, `audit-logs.ts`, `test-data.ts`

## 6. 핵심 서비스 (`src/server/services/`)

- `doc-engine/` — 규칙 기반으로 엑셀·hwpx 문서를 자동 채워 생성 (`rule-engine.ts`, `excel-adapter.ts`, `hwpx-adapter.ts`, `doc-sequence.ts`)
- `ml/` — 수요예측 모델 학습/추론 (`featureBuilder.ts`, `trainModel.ts`, `predict.ts`)
- `inferDemand.ts` / `inferReason.ts` / `demand-forecast.ts` — 수요/사유 추론
- `stock-out-service.ts` / `stock-out-receipt-service.ts` — 출고·입고 확정 로직 (FIFO 로트 처리 등)
- `patient-import.ts` / `excel-sync.ts` — 엑셀 파일 감시/동기화로 환자 데이터 반영
- `perm-bootstrap.ts` — 권한 초기 세팅
- `pdf.ts` — PDF 생성(pdfkit)
- `backup.ts` / `health-check.ts` — DB 백업, 헬스체크
- `webhook-service.ts`, `llm-service.ts`, `ai-function-router.ts` — 외부 연동/AI 기능

## 7. DB 구조 (`prisma/schema.prisma`, 60여 개 모델)

주요 그룹:

- **조직/권한**: Department, User, Role, Permission, RolePermission, UserRole, DeptCategoryPermission, DeptItemPermission
- **품목/거래처**: Item, ItemUnitsPerPack, ItemVendorMap, PriceHistory, Vendor, ExternalPurchaseRecord, ItemCategoryMaster
- **환자/병동**: Patient, PatientStat, PatientDiseaseCode, DiseaseCode, WardRoom, **WardRoomBoard**(병실현황판 — 화면에 실제 보이는 데이터는 여기), PatientEvent, PatientTreatment, PatientItemUsage, TreatmentType, TreatmentSupplyMap
- **신청/승인**: WardRequest, WardRequestItem, ApprovalAction, ApprovalActionItem, RequestSchedule
- **발주/입출고**: PurchaseOrder, PurchaseOrderItem, PurchaseOrderSource, OrderRouting, PurchaseDecision, ProposalDocument, GoodsReceipt, StockInItem, PendingReceiptFollowUp, StockOut, StockOutItem, StockOutFollowUp
- **재고**: Inventory, InventoryLocation, InventoryLot, StockOutLotAllocation, UsageBaseline
- **문서엔진**: DocTemplate, DocTemplateRule, DocSubmission, DocSequenceCounter
- **기타**: AuditLog, AppSetting, BackupRecord, IncinerationEntry, IncinerationMonthlyOverride, EquipmentUnit, EquipmentRepair, ExpenseScopeMaster, DeptCalendarEvent

> 참고: `PatientCharge`, `PatientPayment`, `Referral` 모델은 스키마에 남아있지만, 실제 수납/청구·의뢰서
> 업무는 현재 **EMR 시스템에서 처리**하며 이 프로그램에서는 사용하지 않습니다(화면 라우트도 제거됨).

## 8. 개발 시 꼭 알아야 할 특이사항

- **병실현황판 데이터는 `patients` 테이블이 아니라 `ward_room_boards` 테이블이 우선.** `patients`만 업데이트하면 화면에 반영 안 됨.
- **물품 통계의 환자수는 "일평균"** 기준(`ward_room_boards`의 일자별 평균). "지금 입원중인 인원수" 카운트 방식이 아님.
- **화면에 내부 코드 노출 금지.** `DIAPER`, `diaper_state`, `IN_HOUSE` 같은 내부 코드는 반드시 한글 라벨로 변환해서 표시.
- **신청 제출 실패 시 DRAFT 상태로 남기지 않음.** 신청자 입장에서는 "임시저장" 개념이 없고, 실패하면 폐기하고 실제 오류를 그대로 보여줌.
- **분류(중분류)는 사용자가 직접 추가 가능.** 접두어 코드 + 런타임 레지스트리 방식이라 통계/코드 수정 없이 확장됨 (`item-categories`, `ItemCategoryMaster`).
- **한글 폰트**: PDF 한글 출력을 위해 `fonts/` 폴더에 Noto Sans KR 폰트 파일이 필요 (`fonts/README.md` 참고, 라이선스상 저장소에는 폰트 파일 자체는 미포함).

## 9. 최근 추가된 주요 기능 (가장 최근 커밋 기준)

- **구매결정(Purchase Decisions)** — `purchase-decisions.ts`, `PurchaseDecisionsPage.tsx`: 구매결의서 생성 워크플로우. 실행 시 `구매결의서양식/구매결의서 양식.xlsx` 템플릿을 직접 읽어서 문서를 생성하므로 이 파일은 저장소에 반드시 남아있어야 함.
- **발주라우팅(Order Routing)** — `order-routing.ts`, `OrderRoutingPage.tsx`: 발주를 어느 경로/거래처로 보낼지 배분하는 로직.
- **원가분석/정산(Cost Analysis/Reconcile)** — `cost-analysis.ts`, `cost-reconcile.ts`: 품목 원가 분석 및 정산 대사.
- **문서엔진(Doc Engine)** — `src/server/services/doc-engine/`: 규칙(rule-engine) 기반으로 엑셀/hwpx 문서를 자동 채워 생성하는 범용 엔진. `DocTemplate`/`DocTemplateRule`/`DocSubmission` 모델과 연동.
- **부서별 재고(Dept Inventory)** — `DeptInventoryPage.tsx`: 부서 단위로 재고를 별도 추적하는 신규 화면.
- **품목 재분류(Recategorize)** — `RecategorizePage.tsx`: 기존 품목을 새 분류 체계로 일괄 재분류.

## 10. 이어서 개발이 가능한가?

가능합니다. 이 문서 + 코드베이스만으로:

- 새 화면/API를 추가할 때 어느 폴더에 파일을 두고 `App.tsx`에 라우트를 어떻게 등록하는지 패턴을 그대로 따라 하면 됩니다 (기존 라우트들이 일관된 구조 — `ProtectedRoute` + `menuKey` + 권한 체크).
- DB 변경이 필요하면 `prisma/schema.prisma` 수정 → `npx prisma migrate dev`로 마이그레이션 생성.
- 기존 기능을 고칠 때는 위 "라우트 ↔ 화면" 표와 "라우트 그룹" 목록으로 관련 파일을 빠르게 찾을 수 있습니다.

다만 문서만으로는 각 기능의 세부 비즈니스 규칙(예: FIFO 로트 배분 알고리즘, 문서엔진 규칙 문법)까지는
다루지 않으므로, 해당 기능을 고칠 때는 관련 서비스 파일(`stock-out-service.ts`, `doc-engine/rule-engine.ts` 등)의
코드와 테스트 파일(`*.test.ts`)을 같이 참고하는 걸 권장합니다.
